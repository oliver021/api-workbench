{
  "name": "api-workbench",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --filter cli dev",
    "dev:ui": "pnpm --filter ui dev",
    "build": "pnpm --filter '!ui' build && pnpm --filter ui build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
